export enum Sex {
  FEMALE = 'female',
  MALE = 'male',
  UNKNOWN = 'unknown',
}

export enum UserStatus {
  VERIFYING = 0,
  ACTIVE = 1,
  INACTIVE = 2,
  FAIL = -1,
}

export enum UserRole {
  USER,
  ADMIN,
}